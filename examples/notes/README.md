# notes example

Minimal stdio MCP server using mcpkit **envelope + adapter** only (no proxy pipeline).

```bash
go run ./examples/notes
```

Uses `adapter.OK`, `adapter.ErrorCode`, `present.WrapToolList`, and `present.RedactText`.
